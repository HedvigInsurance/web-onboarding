mutation EditQuote($input: EditQuoteInput!) {
  editQuote(input: $input) {
    ... on CompleteQuote {
      id
      price {
        amount
        currency
      }
    }
    ... on UnderwritingLimitsHit {
      limits {
        description
      }
    }
  }
}
