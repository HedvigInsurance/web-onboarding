query PotentialCrossSells($locale: Locale!) {
  activeContractBundles {
    potentialCrossSells(locale: $locale) {
      type
      contractType
      webTitle
      webDescription
      imageUrl
      callToAction
    }
  }
}
