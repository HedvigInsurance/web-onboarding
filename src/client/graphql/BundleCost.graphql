query BundleCost($id: ID!) {
  quoteCart(id: $id) {
    bundle {
      bundleCost {
        monthlyGross {
          amount
        }
        monthlyNet {
          amount
        }
        monthlyDiscount {
          amount
        }
        freeUntil
      }
    }
  }
}
