query ContactInformation($id: ID!) {
  quoteCart(id: $id) {
    id
    bundle {
      possibleVariations {
        id
        bundle {
          quotes {
            firstName
            lastName
            ssn
            email
          }
        }
      }
    }
  }
}
