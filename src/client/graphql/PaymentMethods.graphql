query PaymentMethods($id: ID!) {
  quoteCart(id: $id) {
    id
    bundle {
      quotes {
        id
      }
    }
    paymentConnection {
      id
      providers {
        __typename
        ... on Adyen {
          availablePaymentMethods
        }
      }
    }
  }
}
